# ── Core CV ──────────────────────────────────────────────────────────────────
torch==2.7.1
torchvision==0.22.1
torchaudio==2.7.1
numpy==2.2.6
opencv-python==4.12.0.88
pillow==11.0.0

# ── Detection + Export ────────────────────────────────────────────────────────
ultralytics==8.4.80
onnx==1.21.0
onnxruntime==1.24.4
# TensorRT: instalar desde NVIDIA en el nodo GPU (vast.ai)
#   pip install tensorrt --index-url https://pypi.ngc.nvidia.com

# ── Segmentación ─────────────────────────────────────────────────────────────
# SAM2: instalar desde fuente (no está en PyPI)
#   pip install git+https://github.com/facebookresearch/sam2.git

# ── VLM (Qwen2-VL-7B / LLaVA-Next) ─────────────────────────────────────────
transformers==4.53.2
accelerate==1.13.0
safetensors==0.5.3
huggingface-hub>=0.30.0,<1.0
tokenizers==0.21.2
sentencepiece==0.2.1
bitsandbytes>=0.45.0          # 4-bit quantization — FALTANTE

# ── Structured output ─────────────────────────────────────────────────────────
instructor>=1.7.0             # FALTANTE
openai>=1.50.0                 # cliente OpenAI-compatible — OpenRouter (describe api + measure)
pydantic==2.11.7
pydantic-settings==2.14.2

# ── Agente / Orquestación ────────────────────────────────────────────────────
langgraph>=0.2.0              # FALTANTE — core del pipeline
langchain>=0.3.0              # FALTANTE
langchain-core>=0.3.0         # FALTANTE
langsmith>=0.1.0              # FALTANTE — trazas del agente

# ── API ───────────────────────────────────────────────────────────────────────
fastapi==0.135.3
uvicorn==0.44.0
python-multipart==0.0.24

# ── Demo UI ───────────────────────────────────────────────────────────────────
gradio>=4.0.0                  # demo visual del pipeline (ui/app.py)

# ── MLOps ─────────────────────────────────────────────────────────────────────
mlflow==3.11.1
dvc==3.67.1

# ── Data / Utils ──────────────────────────────────────────────────────────────
datasets==4.0.0
pandas==2.3.1
Pillow==11.0.0
tqdm==4.67.1
python-dotenv==1.2.2
