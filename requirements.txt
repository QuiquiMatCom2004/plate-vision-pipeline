# ── Core CV ──────────────────────────────────────────────────────────────────
torch==2.7.1
torchvision==0.22.1
torchaudio==2.7.1
numpy==2.2.6
opencv-python==4.12.0.88
pillow==11.0.0

# ── Detection + Export ────────────────────────────────────────────────────────
ultralytics==8.4.80
onnx==1.21.0
onnxruntime==1.24.4
# TensorRT: instalar desde NVIDIA en el nodo GPU (vast.ai)
#   pip install tensorrt --index-url https://pypi.ngc.nvidia.com

# ── Segmentación ─────────────────────────────────────────────────────────────
# SAM2: instalar desde fuente (no está en PyPI)
#   pip install git+https://github.com/facebookresearch/sam2.git

# ── VLM (Qwen2-VL-7B / LLaVA-Next) ─────────────────────────────────────────
# >=5.0.0 — versión mínima que acepta huggingface-hub>=1.0 (lo exige
# gradio>=5.27, el primero sin el bug de auto-docs con pydantic>=2.10.6).
# Companions (safetensors/tokenizers/accelerate) sin pin exacto — que uv
# resuelva la versión que transformers==5.17.0 realmente pide.
transformers==5.17.0
accelerate>=1.13.0
safetensors>=0.8.0
huggingface-hub>=1.3.0,<2.0
tokenizers>=0.21.2
sentencepiece==0.2.1
bitsandbytes>=0.45.0          # 4-bit quantization — FALTANTE

# ── Structured output ─────────────────────────────────────────────────────────
instructor>=1.7.0             # FALTANTE
openai>=1.50.0                 # cliente OpenAI-compatible — OpenRouter (describe api + measure)
pydantic==2.11.7
pydantic-settings==2.14.2

# ── Agente / Orquestación ────────────────────────────────────────────────────
langgraph>=0.2.0              # FALTANTE — core del pipeline
langchain>=0.3.0              # FALTANTE
langchain-core>=0.3.0         # FALTANTE
langsmith>=0.1.0              # FALTANTE — trazas del agente

# ── API ───────────────────────────────────────────────────────────────────────
fastapi==0.135.3
uvicorn==0.44.0
python-multipart==0.0.24

# ── Demo UI ───────────────────────────────────────────────────────────────────
# >=5.27.0 — primera versión sin el bug de auto-docs con pydantic>=2.10.6
# (gradio<5.27 choca con pydantic==2.11.7, fijado abajo para fastapi/instructor).
gradio>=5.27.0,<6.0.0          # demo visual del pipeline (ui/app.py)

# ── MLOps ─────────────────────────────────────────────────────────────────────
mlflow==3.11.1
dvc==3.67.1

# ── Data / Utils ──────────────────────────────────────────────────────────────
datasets==4.0.0
pandas==2.3.1
tqdm==4.67.1
python-dotenv==1.2.2
